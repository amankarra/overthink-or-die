{
  "name": "overthink-or-die",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test:smoke": "playwright test --project=chromium",
    "test:smoke:prod": "npm run build && SMOKE_PROD=1 playwright test --project=chromium"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "@playwright/test": "1.55.1",
    "typescript": "5.9.2",
    "vite": "7.1.7"
  }
}
